{% extends "EcoleSiteBundle::layout.html.twig" %}
 
   {% block  content %}
      <div class="row">
        <div class="col-sm-12"> 
            <h4 class="titreM green">Projet pédagogique</h4>
            <div id="contenu">
                <div class="w3-container">
                    {% for contenu in contenus %}
                        {% if contenu.type == 'pedagogique' %}
                            {{ contenu.contenu|raw }}
                        {% endif %}
                    {% endfor %}
                 </div>
            </div>
            </div> 
      </div> 
       {% block javascript %}     
          <script>
              $(function(){
                 $('#notrecole').addClass('active');
                 $('#home').removeClass('active');
                 $('#reseaux').hide();
                 $('#pedag').addClass('yellow');
              });
          </script>
        {% endblock %}
   {% endblock %}  
 

     

